<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.eds.wx.common">    <select id="queryWxExpiredToken" resultType="java.util.Map">        SELECT appId,appSecret        FROM t_b_wx_access_token        WHERE dataStatus = 1 AND NOW() > DATE_SUB(expireDate,INTERVAL 10 MINUTE )    </select></mapper>